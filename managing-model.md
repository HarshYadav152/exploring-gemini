### Temperature (`Creativity control`)
- high (wild, creative, unpredictable)
- low (stable, accurate, deterministic)

### Top - p (`Control word choices`)
Reduce P = 1 full range of words
P = 0.5 only most likely word

> balanced 
    > T = 1.3
    > P = 0.8

### Structured output
controls how ai gives output.
we can define a json schema in which output is given

### Code Execution
execute code snipptes

### Grounding Resuts
from google search results

### URL context
refer provided url

### Function calling